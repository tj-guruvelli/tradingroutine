# Secrets — NEVER commit
.env
*.env
!env.template

# ClickUp local fallback (may contain notification text) — root-anchored so it
# does not also match .claude/commands/daily-summary.md (Windows git is case-insensitive)
/DAILY-SUMMARY.md

# Noise
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# Local orchestration artifacts (claude-flow) — not part of this repo's history
.claude-flow/
.swarm/
ruvector.db

# Runtime output — trade data, safety-check logs, scan results, routine logs.
# Generated locally/in-cloud on every run; not source, not committed.
/data/
/logs/

# graphify concept-graph output (built by another agent in this repo) — never committed
graphify-out/
