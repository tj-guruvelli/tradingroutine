---
description: Show the loop help menu — how to wake up every part of this pipeline
---

Print the contents of `docs/LOOP-HELP.md` verbatim to the terminal. No changes,
no other side effects. If `docs/LOOP-HELP.md` is missing, tell the user to run
the pipeline setup first.
